odoo.define('smart_system2.main', function (require) {
"use strict";

	var models = require('smart_system2.models');
	var screens = require('smart_system2.screens');
	var popups = require('smart_system2.popups');
	var db = require('smart_system2.db');
	var chrome = require('smart_system2.chrome');

});